anthropic>=0.40.0
requests>=2.31.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
lxml>=5.0.0
fake-useragent>=1.5.0
fpdf2>=2.8.0
python-dotenv>=1.0.0

# Web GUI (python app.py)
flask>=3.0.0

# Optional — only needed for --pdf-style modern (HTML-based PDFs with custom fonts).
# Default CLI + GUI ship with --pdf-style classic (fpdf2 only), so this stays opt-in.
# pip install playwright && playwright install chromium
# playwright>=1.40.0
